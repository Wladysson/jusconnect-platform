apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: jusconnect
  labels:
    app: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: jusconnect
            - name: POSTGRES_USER
              value: jusconnect
            - name: POSTGRES_PASSWORD
              value: jusconnect